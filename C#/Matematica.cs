using System;
class Matematica
{
	public static int Main(string[] args)
	{
		int x = Math.Max(3, 2);
		Console.WriteLine("Max din 1,2 e " + x);
		int y = (int)Math.Pow(5, 2);
		Console.WriteLine("5 la puterea 2 este " + y);
		double z = Math.E;
		Console.WriteLine(z);
		Console.WriteLine("PI = " + Math.PI);
		return 0;
	}


}
   